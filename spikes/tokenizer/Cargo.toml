[package]
name = "spike-tokenizer"
version = "0.0.0"
edition = "2021"
publish = false

# Phase-0 spike 0a-3 (RISK-11): nomic tokenizer.json via the `tokenizers` crate;
# token counting + full-coverage chunking (no silent truncation).

[dependencies]
tokenizers = { workspace = true }
