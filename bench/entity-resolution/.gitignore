# Raw benchmark datasets are NOT committed — fetched on demand from the
# Leipzig ER benchmark page (see README "Fetch the data"). Re-fetchable, large,
# and not ours to redistribute.
data/

# Local handlebars install (see README "Dependencies"). The dependency is
# declared in package.json; node_modules is environment-local, not committed.
node_modules/

# Derived candidate cache (gathered cross-NN lists; re-derivable from the engine).
.candidate-cache/
